# URL of the deployed backend (ui-sim-app-back).
# Set this to your hosted backend's URL. In Vercel, also add it under
# Project Settings -> Environment Variables (name: VUE_APP_SERVER_URL),
# which overrides this file at build time.
VUE_APP_SERVER_URL=https://your-backend-host.example.com
